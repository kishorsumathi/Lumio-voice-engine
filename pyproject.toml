[project]
name = "anchor-voice"
version = "0.1.0"
description = "Audio transcription app with speaker diarization, live streaming, and shared audio library"
requires-python = ">=3.13"
dependencies = [
    "deepgram-sdk>=6.1.1",
    "python-dotenv>=1.2.2",
    "streamlit>=1.56.0",
]
